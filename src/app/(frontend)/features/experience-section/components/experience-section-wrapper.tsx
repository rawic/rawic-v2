'use client'

import { Element } from 'react-scroll'

export const ExperienceSectionWrapper = ({ children }: { children: React.ReactNode }) => {
  return <Element name="experience">{children}</Element>
}
